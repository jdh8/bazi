# CLAUDE.md

## UI/UX 判斷

使用者描述觀感（「看起來偏下」「顏色太像」）時，不直接照改。先用反映多數人感知的指標實際量測：

- 顏色差異：CIEDE2000，ΔE00 < 1 視為不可辨，> 2.3 視為可辨
- 可讀性：WCAG 對比度，內文 ≥ 4.5:1，大字與非文字元素 ≥ 3:1
- 文字置中：墨跡重心相對幾何中心的偏移（以 em 計），外框中心為輔

量測結果支持觀感才改；不支持就回報數據，由使用者決定。找不到適用指標時，說明並詢問。

使用者明確指定的規格（數值、顏色、位置）直接照做，不受此條限制。
